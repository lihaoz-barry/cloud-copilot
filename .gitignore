node_modules/
npm-debug.log*
.DS_Store
.env
data/
# pure acceleration layer — rebuilt from GitHub on demand, never source of truth
data/gh-cache.json
server.js.bak
server.log

# secrets / local deploy config — never commit
*.p8
setup/deploy.env


# playwright-mcp scratch output
.playwright-mcp/
